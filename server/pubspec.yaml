name: manari_server
description: Sync API server for the Notes & Reminders app.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
